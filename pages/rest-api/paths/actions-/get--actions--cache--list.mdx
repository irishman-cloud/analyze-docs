import { Tabs } from 'nextra/components'

# GET - `/repos/{owner}/{repo}/actions/caches`

List GitHub Actions caches for a repository

## Parameters

| Name | Description | Type | Example |
| :---- | :---- | :---- | :---- |
| owner <span class="bg-blue-100 text-blue-800 text-xs font-medium me-2 px-2.5 py-0.5 rounded dark:bg-blue-900 dark:text-blue-300">Required</span> | The account owner of the repository. The name is not case sensitive. | string in path | `undefined` |
| repo <span class="bg-blue-100 text-blue-800 text-xs font-medium me-2 px-2.5 py-0.5 rounded dark:bg-blue-900 dark:text-blue-300">Required</span> | The name of the repository. The name is not case sensitive. | string in path | `undefined` |
| per_page  | The number of results per page (max 100). | integer in query | `undefined` |
| page  | Page number of the results to fetch. | integer in query | `undefined` |
| ref  | The Git reference for the results you want to list. The `ref` for a branch can be formatted either as `refs/heads/<branch name>` or simply `<branch name>`. To reference a pull request use `refs/pull/<number>/merge`. | string in query | `undefined` |
| key  | An explicit key or prefix for identifying the cache | string in query | `undefined` |
| sort  | The property to sort the results by. `created_at` means when the cache was created. `last_accessed_at` means when the cache was last accessed. `size_in_bytes` is the size of the cache in bytes. | string in query | `undefined` |
| direction  | The direction to sort the results by. | string in query | `undefined` |

## Responses

Blah blah blah.

<Tabs items={["200"]}>

<Tabs.Tab>

Response

```json

{
    "total_count": 2,
    "actions_caches": [
        {
            "id": 2,
            "ref": "",
            "key": "",
            "version": "",
            "last_accessed_at": "",
            "created_at": "",
            "size_in_bytes": 1024
        }
    ]
}

```

</Tabs.Tab>

</Tabs>